run make with default run, ipv4 or ipv6 to compile files and run for floodng.
arguments: -t <target address> -p <targeted pot> -r (for ipv4 set flood from tcp to udp)
use ctrl+c to stop flood.
if given bad arguments the program can seen running but is actualy not sending anything (ipv6)

